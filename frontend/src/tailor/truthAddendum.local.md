## Truth addendum (local-only, not committed)

Use this to list **true** skills/experience you want Tailor/ATS to use even if they aren’t represented in the `bio/` bank yet.

Guidelines:
- Prefer short, specific statements with at least one concrete detail (what, where, when, how).
- If you’re not comfortable defending it in an interview, don’t include it.

### Skills/tools (true)

- Real Torch: used Docker for containerization; deployed/operated workloads with Kubernetes; worked with Azure (services + deployment).
- Example: Used Airflow to schedule ETL DAGs for a personal project (2025).
- Example: Built dashboards in Tableau and Power BI for coursework and side projects.
- Example: Containerized services with Docker; basic Kubernetes exposure (deployments/services).

### Projects / experience claims (true)

- Example: Built a data pipeline ingesting X → Y, stored in Postgres, queried with SQL.
- Example: Implemented model evaluation metrics (precision/recall, ROC AUC) for classification project.

### Keywords you want to target (optional)

- Example: Spark, dbt, Kafka, Snowflake, BigQuery, Redshift, Terraform

