# Tasks: bio-bank

- [x] Read `.specify/README.md`, `architecture/index.md`, `memory/*`
- [x] Extend `GET /api/bio/bank` for summaries + certifications
- [x] Extend `BioBank` + `makeBankForPrompt`
- [x] Bio bank viewer: readable sections + contact + raw JSON
- [x] Update architecture + `api-shapes.md`
- [x] `npm run lint` and `npm run build` in `frontend/`
- [x] Reset `.specify/llm-prompt.txt` feature placeholders
- [x] Move folder to `features/completed/bio-bank/`
- [x] Split `BioBankViewer` into `frontend/src/components/bioBankViewer/` (hook + presentational modules); refresh `.specify` architecture + bio-bank spec/plan
