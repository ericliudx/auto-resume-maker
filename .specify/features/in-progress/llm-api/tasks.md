# llm-api — tasks

- [x] Create `spec.md` with flows + constraints
- [x] Create `plan.md` with boundaries + verification
- [x] Add `api-shapes.md` (local request/response contract) and keep it minimal
- [x] Implement local-only LLM adapter (Groq) with normalized result + error shapes
- [x] Add a local API/proxy route so the browser never sees `GROQ_API_KEY`
- [x] Add a minimal frontend smoke path to trigger one request and render the text
- [x] Verify locally using the exact commands in `.specify/architecture/index.md`
- [ ] Move this folder to `.specify/features/completed/llm-api/` after verification

