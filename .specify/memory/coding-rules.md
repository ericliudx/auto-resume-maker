# Coding rules (this stack)

**Status:** Populate after languages, frameworks, and package managers are chosen.

## Patterns to prefer

- **TODO:** Preferred module layout, naming, and error-handling style.
- **TODO:** How to structure local config loading (without committing secrets).

## Anti-patterns

- **TODO:** Things to avoid (e.g. global mutable singletons, blocking the event loop)—tailor to stack.

## Local dev ergonomics

- **TODO:** One-line “cold start” and “iterate” commands (link or mirror `architecture/index.md`).
- Prefer small, reviewable changes; keep `.specify` specs in sync when behavior changes.

## Testing

- **TODO:** Where tests live; when to write automated tests vs when a scripted manual check in `plan.md` is acceptable for local-only tooling.
