You are an AI coding assistant working in a NEW repo that uses lightweight spec-driven docs under `.specify/`.

This is a **locally-hosted app** (dev machine / LAN). Avoid cloud/multi-tenant assumptions unless explicitly requested.

## First: bootstrap `.specify/` if missing

If `.specify/` does not exist yet, create:

- `.specify/README.md`: what this folder is, how to use it
- `.specify/llm-prompt.txt`: this reusable feature-work template
- `.specify/architecture/index.md`: current/planned module boundaries + local run commands (use TODOs if unknown)
- `.specify/memory/constitution.md`: non-negotiables (quality bar, testing expectations, secrets policy)
- `.specify/memory/coding-rules.md`: practical code conventions for this repo
- `.specify/features/README.md`: feature lifecycle + naming rules
- `.specify/features/in-progress/.gitkeep`
- `.specify/features/completed/.gitkeep`

Keep these docs short; prefer TODO placeholders over guessing.

## Feature context (fill this in first)

- Feature name: <kebab-case-feature-name>
- Goal: <one sentence>
- Non-goals / out of scope: <bullets>
- Affected area(s): <frontend/backend/cli/etc>
- Links (optional): <issues/docs/figma/etc>

## Determine scope + boundaries

- Follow `.specify/architecture/` boundaries.
- If boundaries aren’t documented yet, update `.specify/architecture/index.md` first with:
  - Area ownership (what lives where)
  - Dependency rules between areas (what can import what)
  - Local run/test commands (or TODO)

## Create feature folder

Create:

- `.specify/features/in-progress/<feature-name>/`

Add:

- `spec.md`: behavior contract (user goal, flows, edge cases/invariants, “must not change”, non-functional constraints)
- `plan.md`: technical approach (structure, ownership boundaries, implementation sequence, verification plan)
- `tasks.md`: checklist (mark `[x]` as you finish items)

Add `api-shapes.md` only if request/response shape matters:

- You are adding/changing endpoints, or
- You rely on non-trivial response fields, or
- You need to lock down params/bodies to avoid regressions

If you skip it, say why in `plan.md`.

## Update architecture docs (only when needed)

Update:

- `.specify/architecture/index.md` and/or the owning doc under `.specify/architecture/...`

Add `patch-notes.md` when you change responsibilities or API shapes, or want durable invariants recorded.

## Verify + close out (local-only)

- Run the project locally and verify `spec.md` behaviors
- Run whatever build/test commands exist (add them to `architecture/index.md` if missing)
- Cleanup dead code/temp files/stale links

When done and verified:

- Move `.specify/features/in-progress/<feature>/` → `.specify/features/completed/<feature>/`