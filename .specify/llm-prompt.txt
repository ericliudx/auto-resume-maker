You are an AI coding assistant working in a repo that uses lightweight spec-driven docs under `.specify/`.

This is a **locally-hosted app**. Avoid cloud/multi-tenant assumptions unless explicitly requested.

IMPORTANT: Do **not** edit the structure (section headings/order) of `.specify/llm-prompt.txt` unless I specifically instruct you to.

## First: align with existing `.specify/`

- Read `.specify/README.md`, `.specify/architecture/index.md`, and `.specify/memory/*` before planning.
- Follow the boundaries and conventions documented there. If something is missing/unclear, add a small doc update rather than guessing.

## Feature context (fill this in first)

- Feature name: _(kebab-case)_
- Goal: _(one sentence user outcome)_
- Non-goals / out of scope:
  - _(explicit boundaries)_
- Affected area(s): _(repo paths / subsystems)_
- Links (optional):
  - _(paths to vision, architecture, related feature folders)_

## Determine scope + boundaries

- Follow `.specify/architecture/` boundaries.
- If boundaries need updating, update `.specify/architecture/index.md` (and/or the owning area doc) before implementation.

## Create feature folder

Create:

- `.specify/features/in-progress/<feature-name>/`

Add:

- `spec.md`: behavior contract (user goal, flows, edge cases/invariants, “must not change”, non-functional constraints)
- `plan.md`: technical approach (structure, ownership boundaries, implementation sequence, verification plan)
- `tasks.md`: checklist (mark `[x]` as you finish items)

Add `api-shapes.md` only if request/response shape matters:

- You are adding/changing endpoints, or
- You rely on non-trivial response fields, or
- You need to lock down params/bodies to avoid regressions

If you skip it, say why in `plan.md`.

## Update architecture docs (only when needed)

Update:

- `.specify/architecture/index.md` and/or the owning doc under `.specify/architecture/...`

Add `patch-notes.md` when you change responsibilities or API shapes, or want durable invariants recorded.

## Verify + close out (local-only)

- Run the project locally and verify `spec.md` behaviors
- Run whatever build/test commands exist (don’t invent commands; if missing, add TODOs to architecture docs)
- Cleanup dead code/temp files/stale links
- Cleanup this `.specify/llm-prompt.txt` (remove or replace any feature-specific placeholders before finishing)

When done and verified:

- Move `.specify/features/in-progress/<feature>/` → `.specify/features/completed/<feature>/`